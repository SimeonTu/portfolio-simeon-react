# Cloudflare Pages Configuration
# Note: This file is optional for Cloudflare Pages deployment
# Cloudflare Pages can auto-detect Vite projects and will use:
# - Build command: npm run build
# - Build output directory: dist
# - Node version: 18.x or higher (configure in dashboard)
#
# The _redirects file in public/ folder handles SPA routing
#
# For manual deployment using Wrangler CLI:
# wrangler pages deploy dist

name = "portfolio-simeon-react"
compatibility_date = "2024-01-01"
